export default function LoginPage() {
  return (
    <main className="flex-1 flex items-center justify-center min-h-screen bg-surface">
      <p className="text-text-muted">Login de administrador — próximamente</p>
    </main>
  )
}
